package main.java.interpreter;

public enum TokenType {
    // char tokens
    L_PAREN, R_PAREN, DOT, STAR, SLASH, PLUS, MINUS, CARET,

    // literals
    NUMBER,

    // keywords (add in the future)
}
